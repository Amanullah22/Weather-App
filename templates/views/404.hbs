<!DOCTYPE html>
<html>
    <head>
        <title>404 Forbidden</title>
        <link rel="stylesheet" href="./css/styles.css">
    </head>

    <body>
        {{>header}}
        <h1>{{ errorMessage }}</h1>
        {{>footer}}
    </body>
</html>